<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:layout_margin="20dp"
    tools:context=".MainActivity">

    <TextView
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="WELCOME TO CAR SERVICE"
        android:textAlignment="center"
        android:textColor="@color/purple_700"
        android:textSize="24dp"
        android:fontFamily="sans-serif-medium"
        android:textStyle="bold"
        android:id="@+id/weltv"
        app:layout_constraintTop_toTopOf="parent"/>

    <ImageView
        android:layout_width="150dp"
        android:layout_marginTop="10sp"
        app:layout_constraintLeft_toLeftOf="parent"
        app:layout_constraintRight_toRightOf="parent"
        app:layout_constraintTop_toBottomOf="@+id/weltv"
        android:layout_height="150dp"
        android:id="@+id/image"
        android:src="@drawable/carservice"
        />

    <Button
        android:id="@+id/button"
        app:layout_constraintTop_toBottomOf="@+id/image"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="User Login"
        android:background="@drawable/buttondesign"
        android:onClick="gotouserlogin"
        android:layout_marginTop="30dp"
        />
    <Button
        android:id="@+id/button1"
        app:layout_constraintTop_toBottomOf="@+id/button"
        android:layout_width="match_parent"
        android:background="@drawable/buttondesign"
        android:layout_height="wrap_content"
        android:text="New User"
        android:onClick="gotousersignup"
        android:layout_marginTop="30dp"
        />

    <Button
        android:id="@+id/button2"
        app:layout_constraintTop_toBottomOf="@+id/button1"
        android:background="@drawable/buttondesign"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Service Provider Login"
        android:onClick="gotoproviderlogin"
        android:layout_marginTop="30dp"
        />


</androidx.constraintlayout.widget.ConstraintLayout>